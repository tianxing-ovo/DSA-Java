# DSA-Java

> 基于 Java 的数据结构算法与 LeetCode 题解

<p align="center">
  <img src="https://img.shields.io/badge/Language-Java%208+-blue.svg" alt="Java">
  <img src="https://img.shields.io/badge/Build-Maven%203+-orange.svg" alt="Maven">
  <img src="https://img.shields.io/badge/Test-JUnit%205-green.svg" alt="JUnit 5">
  <img src="https://img.shields.io/badge/License-MIT-brightgreen.svg" alt="License">
</p>

---

## 目录

- [项目特性](#项目特性)
- [项目目录结构](#项目目录结构)
- [核心模块详解](#核心模块详解)
  - [基础数据结构](#基础数据结构)
  - [进阶数据结构](#进阶数据结构)
  - [经典排序算法](#经典排序算法)
  - [系统设计与业务实战](#系统设计与业务实战)
  - [通用工具](#通用工具)
- [开源协议](#开源协议)

---

## 项目特性

- **规范设计**：标准面向对象与泛型封装且代码精简命名清晰
- **算法演进**：完整呈现经典排序从基础版本到多重优化的迭代历程
- **理论支撑**：融入布隆过滤器最优数学推导与并查集路径压缩优化
- **测试完备**：核心结构与算法全量配备严谨的 JUnit 5 单元测试
- **业务实战**：基于 Stream 流式递归落地真实电商多级分类树形构建

---

## 项目目录结构

```text
DSA-Java
├── .mvn/
├── pom.xml
├── LICENSE
├── README.md
└── src
    ├── main/java
    │   ├── leetcode/
    │   │   ├── kmp/
    │   │   ├── 动态规划/
    │   │   ├── 双指针/
    │   │   ├── 哈希/
    │   │   ├── 快速幂/
    │   │   └── 贪心/
    │   ├── util/
    │   ├── 布隆过滤器/
    │   ├── 并查集/
    │   ├── 排序/
    │   │   ├── 简单排序/
    │   │   └── 高级排序/
    │   ├── 系统设计/
    │   │   └── 三级分类/
    │   ├── 链表/
    │   └── 顺序表/
    └── test/java
        ├── 布隆过滤器/
        ├── 并查集/
        ├── 排序/简单排序/
        ├── 链表/
        └── 顺序表/
```

---

## 核心模块详解

### 基础数据结构

- **顺序表 (`SequenceList<T>`)**
  - **底层实现**：基于泛型数组底层实现且支持迭代器接口遍历
  - **扩缩机制**：支持满载 1.5 倍自动扩容与 1/4 容量减半缩容
  - **操作支持**：提供严格的索引越界检查与完整增删改查操作
- **单向链表 (`SinglyLinkedList<T>`)**
  - **哨兵设计**：引入哨兵头节点统一空表与非空表的插入删除边界处理
  - **功能特性**：支持头尾插法与指定索引的高效增删查操作
- **双向链表 (`DoublyLinkedList<T>`)**
  - **双向哨兵**：采用双向哨兵头尾节点消除前后继指针判空的临界条件
  - **高效操作**：支持双向遍历与高效的头尾及指定位置增删

---

### 进阶数据结构

- **并查集 (`DisjointSet`)**
  - **路径压缩**：查找根节点时触发路径压缩以大幅降低后续查找深度
  - **按秩合并**：基于树规模按秩合并有效防止树形结构退化为单链
  - **连通统计**：支持动态连通性判断与集合总数统计及树结构打印
- **布隆过滤器 (`BloomFilter<T>`)**
  - **理论建模**：基于预期元素量与目标误判率公式自动推导最优参数
  - **高效哈希**：内置 64 位散列函数拆分高低位生成双重哈希组合映射
  - **测试覆盖**：单元测试万级样本实测验证实际误判率稳定符合 1% 理论预期

---

### 经典排序算法

|      排序算法      | 平均时间复杂度 |    最好情况    |    最坏情况    |  空间复杂度  | 稳定性 |
| :----------------: | :-------------: | :-------------: | :-------------: | :-----------: | :----: |
| **冒泡排序** |   $O(n^2)$   |    $O(n)$    |   $O(n^2)$   |   $O(1)$   |  稳定  |
| **选择排序** |   $O(n^2)$   |   $O(n^2)$   |   $O(n^2)$   |   $O(1)$   | 不稳定 |
| **插入排序** |   $O(n^2)$   |    $O(n)$    |   $O(n^2)$   |   $O(1)$   |  稳定  |
| **希尔排序** | $O(n^{1.3})$ | $O(n \log n)$ | $O(n^{1.5})$ |   $O(1)$   | 不稳定 |
| **归并排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ |   $O(n)$   |  稳定  |
| **快速排序** | $O(n \log n)$ | $O(n \log n)$ |   $O(n^2)$   | $O(\log n)$ | 不稳定 |

#### 简单排序

- **冒泡排序 (`bubbleSort`)**
  - **基础版本**：标准双重循环相邻元素比较与交换
  - **提前终止**：引入交换标志位若整轮无逆序对则提前退出
  - **边界优化**：记录每轮最后交换位置作为下一轮比较边界
- **选择排序 (`selectionSort`)**
  - **算法思想**：每轮扫描未排序区间最小值交换至已排序序列尾部
- **插入排序 (`insertionSort`)**
  - **算法思想**：将未排序元素向前扫描并插入到已排序序列合适位置
- **希尔排序 (`shellSort`)**
  - **步长优化**：采用 Hibbard 增量序列分组跨步长插入使数组整体基本有序

#### 高级排序

- **归并排序 (`mergeSort`)**
  - **分治合并**：经典分治递归双指针合并两个有序子数组且时间复杂度稳定
- **快速排序 (`quickSort`)**
  - **单边划分**：随机选取主元通过单指针维护小于基准值的边界区间
  - **双边划分**：随机选取主元左右双指针相向扫描对调减少无效移动

---

### 系统设计与业务实战

- **三级分类树构建 (`listWithTree`)**
  - **业务场景**：数据库平铺类目结构转为前台展示所需的树状多级嵌套数据
  - **流式递归**：利用 Java 8 Stream API 配合递归下钻高效构建多级嵌套树形结构
  - **数据集成**：结合 Jackson 库验证并输出标准树形 JSON 数据

---

### 通用工具

- **元素交换工具 (`SwapUtil`)**
  - **中间变量**：常规通用元素交换且全面支持对象与各类基本数据类型
  - **数学加减**：利用代数加减运算实现整型数值零额外变量交换
  - **异或运算**：利用位运算自反特性实现零额外临时变量交换

---

## 开源协议

本项目采用 [MIT License](LICENSE) 许可证
